import {
  core::c-size {C-Size, c-size},
  core::cell {borrow, cell, make-cell, mutate},
  core::int {max-int},
  core::int.show {show-int},
  core::layer {axiom-T},
  core::list {reverse},
  core::pointer {shift},
  core::rune,
  core::string {byte-length, format, from-text, take-N, with-byte-slice},
  core::string.pointer {get-content-pointer},
  core::string.show {show-string, show-string-N},
  core::string.unsafe {drop-N, get-rune-unchecked, slice-N},
}

foreign {
  memcmp(pointer, pointer, c-size) -> int,
}

data expected-item {
| _Raw-Text(&string)
| _Label(&string)
}

define _show-expected-item(k: expected-item) -> string {
  match k {
  | _Raw-Text(t) =>
    show-string-N(t)
  | _Label(t) =>
    *t
  }
}

data point {
| Point(
    row: int,
    column: int,
  )
}

define show-point(p: point) -> string {
  let Point{row, column} = p;
  format("row {}, column {}", List::[show-int(add-int(row, 1)), show-int(add-int(column, 1))])
}

data parse-error {
| Parse-Error(
    found: string,
    expected: expected-item,
    error-location: point,
  )
}

define report(e: parse-error) -> string {
  let Parse-Error{found, expected, error-location} = e;
  let error-location = show-point(error-location);
  format("parse error at {}:\nexpected:\n  {}\nfound:\n  {}", List::[error-location, _show-expected-item(expected), found])
}

data zonk-kit {
| _Zonk-Kit(
    input-stream: string,
    cursor: cell(int) mix,
    gap-size: cell(int) mix,
    point-cursor: cell(int) mix,
    point-line: cell(int) mix,
    point-column: cell(int) mix,
    expected-is-raw: cell(bool) mix,
    expected-text: cell(text) mix,
  )
}

define make-zonk-kit(input-stream: string) ->> zonk-kit {
  _Zonk-Kit{
    input-stream,
    cursor := make-cell(lift {0}),
    gap-size := make-cell(lift {0}),
    point-cursor := make-cell(lift {0}),
    point-line := make-cell(lift {0}),
    point-column := make-cell(lift {0}),
    expected-is-raw := make-cell(lift {False}),
    expected-text := make-cell(lift {"unknown"}),
  }
}

inline _read-int-cell(r: &cell(int)) -> int {
  borrow(r, (x) => {
    lift {*x}
  })
}

inline _write-int-cell(r: &cell(int), v: int) -> unit {
  mutate(r, (_) => {
    lift {v}
  })
}

inline _read-bool-cell(r: &cell(bool)) -> bool {
  borrow(r, (x) => {
    lift {*x}
  })
}

inline _write-bool-cell(r: &cell(bool), v: bool) -> unit {
  mutate(r, (_) => {
    lift {v}
  })
}

define _get-input-stream(k: &zonk-kit) -> &string {
  tie _Zonk-Kit{input-stream} = k;
  input-stream
}

inline _get-cursor(k: &zonk-kit) -> int {
  tie _Zonk-Kit{cursor} = k;
  _read-int-cell(cursor)
}

inline _set-cursor(k: &zonk-kit, new-cursor: int) -> unit {
  tie _Zonk-Kit{cursor} = k;
  _write-int-cell(cursor, new-cursor)
}

inline _get-gap-size(k: &zonk-kit) -> int {
  tie _Zonk-Kit{gap-size} = k;
  _read-int-cell(gap-size)
}

inline _set-gap-size(k: &zonk-kit, new-gap-size: int) -> unit {
  tie _Zonk-Kit{gap-size} = k;
  mutate(gap-size, (g) => {
    lift {max-int(axiom-T(g), new-gap-size)}
  })
}

inline _reset-gap-size(k: &zonk-kit, new-gap-size: int) -> unit {
  tie _Zonk-Kit{gap-size} = k;
  _write-int-cell(gap-size, new-gap-size)
}

inline _store-expected(k: &zonk-kit, is-raw: bool, t: text) -> unit {
  tie _Zonk-Kit{expected-is-raw, expected-text} = k;
  _write-bool-cell(expected-is-raw, is-raw);
  mutate(expected-text, (_) => {
    lift {t}
  })
}

inline _set-expected-raw(k: &zonk-kit, t: text) -> unit {
  _store-expected(k, True, t)
}

inline _set-expected-label(k: &zonk-kit, l: text) -> unit {
  _store-expected(k, False, l)
}

define _get-expected(k: &zonk-kit) -> expected-item {
  tie _Zonk-Kit{expected-is-raw, expected-text} = k;
  let t =
    from-text(borrow(expected-text, (x) => {
      lift {*x}
    }));
  if _read-bool-cell(expected-is-raw) {
    _Raw-Text(t)
  } else {
    _Label(t)
  }
}

inline _advance-point(line: int, column: int, r: rune) ->> point {
  if eq-rune(r, `\n`) {
    Point(add-int(line, 1), 0)
  } else {
    Point(line, add-int(column, 1))
  }
}

define _resolve-point(k: &zonk-kit, target: int) -> point {
  let input-stream = _get-input-stream(k);
  tie _Zonk-Kit{point-cursor, point-line, point-column} = k;
  let base-cursor = _read-int-cell(point-cursor);
  let scan =
    define go(cursor: int, line: int, column: int) -> point {
      if ge-int(cursor, target) {
        Point(line, column)
      } else {
        let r = get-rune-unchecked(input-stream, cursor);
        let Point(line, column) = _advance-point(line, column, r);
        go(add-int(cursor, core::rune.size(r)), line, column)
      }
    };
  let Point(line, column) =
    if le-int(base-cursor, target) {
      scan(base-cursor, _read-int-cell(point-line), _read-int-cell(point-column))
    } else {
      scan(0, 0, 0)
    };
  _write-int-cell(point-cursor, target);
  _write-int-cell(point-line, line);
  _write-int-cell(point-column, column);
  Point(line, column)
}

define make-parse-error(k: &zonk-kit) ->> parse-error {
  let cursor = _get-cursor(k);
  let gap-size = _get-gap-size(k);
  let error-location = _resolve-point(k, cursor);
  let input-stream = _get-input-stream(k);
  let rest = drop-N(input-stream, cursor);
  let found on rest = show-string(take-N(rest, gap-size));
  let _ = rest;
  Parse-Error{
    expected := _get-expected(k),
    found,
    error-location,
  }
}

alias parsed(a) {
  either(unit, a)
}

alias zonk(a) {
  (&zonk-kit) ->> parsed(a)
}

inline report-unexpected-input<a>(k: &zonk-kit, label: text) -> parsed(a) {
  _set-expected-label(k, label);
  Left(Unit)
}

inline get-point(k: &zonk-kit) -> point {
  _resolve-point(k, _get-cursor(k))
}

constant end-of-input: zonk(unit) {
  (k) =>> {
    let input-stream = _get-input-stream(k);
    let cursor = _get-cursor(k);
    let len = byte-length(input-stream);
    if ge-int(cursor, len) {
      Right(Unit)
    } else {
      _set-gap-size(k, 1);
      _set-expected-label(k, "end of input");
      Left(Unit)
    }
  }
}

inline satisfy(label: text, p: (rune) -> bool) ->> zonk(rune) {
  (k) =>> {
    let input-stream = _get-input-stream(k);
    let len = byte-length(input-stream);
    let cursor = _get-cursor(k);
    if ge-int(cursor, len) {
      _set-expected-label(k, label);
      Left(Unit)
    } else {
      let r = get-rune-unchecked(input-stream, cursor);
      if p(r) {
        _set-cursor(k, add-int(cursor, core::rune.size(r)));
        Right(r)
      } else {
        _set-gap-size(k, 1);
        _set-expected-label(k, label);
        Left(Unit)
      }
    }
  }
}

constant any-rune: zonk(rune) {
  satisfy("any character", (_) => {
    True
  })
}

define _chunk-body(k: &zonk-kit, chunk-string: &string) -> int {
  let input-stream = _get-input-stream(k);
  let cursor = _get-cursor(k);
  let len1 = byte-length(input-stream);
  let len2 = byte-length(chunk-string);
  if lt-int(sub-int(len1, cursor), len2) {
    -1
  } else-if eq-int(len2, 0) {
    1
  } else {
    let input-ptr = shift(get-content-pointer(input-stream), cursor);
    let chunk-ptr = get-content-pointer(chunk-string);
    let match-result = magic external memcmp(input-ptr, chunk-ptr, C-Size(len2));
    if ne-int(match-result, 0) {
      _set-gap-size(k, core::string.length(chunk-string));
      -1
    } else {
      _set-cursor(k, add-int(cursor, len2));
      1
    }
  }
}

inline chunk(chunk-text: text) ->> zonk(unit) {
  (k) =>> {
    let result-flag = _chunk-body(k, from-text(chunk-text));
    if gt-int(result-flag, 0) {
      Right(Unit)
    } else {
      _set-expected-raw(k, chunk-text);
      Left(Unit)
    }
  }
}

inline _scan-while(k: &zonk-kit, !p: (rune) -> bool) -> int {
  let input-stream = _get-input-stream(k);
  let len = byte-length(input-stream);
  let f =
    define consumer(cursor: int) -> int {
      if ge-int(cursor, len) {
        cursor
      } else {
        let r = get-rune-unchecked(input-stream, cursor);
        if p(r) {
          consumer(add-int(cursor, core::rune.size(r)))
        } else {
          cursor
        }
      }
    };
  f(_get-cursor(k))
}

inline take-while(!p: (rune) -> bool) ->> zonk(string) {
  (k) =>> {
    let orig-cursor = _get-cursor(k);
    let end-cursor = _scan-while(k, p);
    _set-cursor(k, end-cursor);
    Right(slice-N(_get-input-stream(k), orig-cursor, sub-int(end-cursor, orig-cursor)))
  }
}

inline take-while-1(!p: (rune) -> bool) ->> zonk(string) {
  (k) =>> {
    let orig-cursor = _get-cursor(k);
    let end-cursor = _scan-while(k, p);
    let size = sub-int(end-cursor, orig-cursor);
    if gt-int(size, 0) {
      _set-cursor(k, end-cursor);
      Right(slice-N(_get-input-stream(k), orig-cursor, size))
    } else {
      _set-expected-label(k, "non-empty string");
      Left(Unit)
    }
  }
}

inline drop-while(!p: (rune) -> bool) ->> zonk(unit) {
  (k) =>> {
    _set-cursor(k, _scan-while(k, p));
    Right(Unit)
  }
}

define _is-ascii-space(r: rune) -> bool {
  if eq-rune(r, ` `) {
    True
  } else-if and::(le-rune(`\u{9}`, r), le-rune(r, `\u{D}`)) {
    True
  } else {
    False
  }
}

constant ascii-space: zonk(unit) {
  drop-while(_is-ascii-space)
}

inline label<a>(l: text, p: zonk(a)) ->> zonk(a) {
  (k) =>> {
    let v = p(k);
    match v {
    | Right(result) =>
      Right(result)
    | Left(_) =>
      _set-expected-label(k, l);
      Left(Unit)
    }
  }
}

inline _rewind(k: &zonk-kit, cursor: int, gap-size: int) -> unit {
  _set-cursor(k, cursor);
  _reset-gap-size(k, gap-size)
}

inline attempt<a>(p: zonk(a)) ->> zonk(a) {
  (k) =>> {
    let cursor = _get-cursor(k);
    let gap-size = _get-gap-size(k);
    match p(k) {
    | Right(result) =>
      Right(result)
    | Left(_) =>
      _rewind(k, cursor, gap-size);
      Left(Unit)
    }
  }
}

inline look-ahead<a>(p: zonk(a)) ->> zonk(a) {
  (k) =>> {
    let cursor = _get-cursor(k);
    let gap-size = _get-gap-size(k);
    match p(k) {
    | Right(result) =>
      _rewind(k, cursor, gap-size);
      Right(result)
    | Left(_) =>
      Left(Unit)
    }
  }
}

define _optional-body<a>(v: parsed(a)) -> parsed(?a) {
  match v {
  | Right(result) =>
    Right(Right(result))
  | Left(_) =>
    Right(Left(Unit))
  }
}

inline optional<a>(p: zonk(a)) ->> zonk(?a) {
  (k) =>> {
    _optional-body(p(k))
  }
}

inline branch<a>(p1: zonk(a), p2: zonk(a)) ->> zonk(a) {
  (k) =>> {
    match p1(k) {
    | Right(result) =>
      Right(result)
    | Left(_) =>
      p2(k)
    }
  }
}

inline not-followed-by<a>(label: text, p: zonk(a)) ->> zonk(unit) {
  (k) =>> {
    let cursor = _get-cursor(k);
    let gap-size = _get-gap-size(k);
    let v = p(k);
    _rewind(k, cursor, gap-size);
    match v {
    | Left(_) =>
      Right(Unit)
    | Right(_) =>
      _set-gap-size(k, 1);
      _set-expected-label(k, label);
      Left(Unit)
    }
  }
}

define _choice<a>(
  k: &zonk-kit,
  label: text,
  candidates: list(zonk(a)),
  fallback: zonk(a),
  default-gap-size: int,
) ->> parsed(a) {
  match candidates {
  | Nil =>
    match fallback(k) {
    | Right(v) =>
      _set-gap-size(k, default-gap-size);
      Right(v)
    | Left(_) =>
      _set-expected-label(k, label);
      Left(Unit)
    }
  | Cons(cand, rest) =>
    match cand(k) {
    | Right(v) =>
      _set-gap-size(k, default-gap-size);
      Right(v)
    | Left(_) =>
      _choice(k, label, rest, fallback, default-gap-size)
    }
  }
}

inline choice<a>(label: text, candidates: list(zonk(a)), fallback: zonk(a)) ->> zonk(a) {
  (k) =>> {
    match candidates {
    | Nil =>
      fallback(k)
    | Cons(p, rest) =>
      let old-cursor = _get-cursor(k);
      match p(k) {
      | Right(v) =>
        Right(v)
      | Left(_) =>
        let new-cursor = _get-cursor(k);
        if eq-int(old-cursor, new-cursor) {
          let gap-size = _get-gap-size(k);
          _choice(k, label, rest, fallback, gap-size)
        } else {
          Left(Unit)
        }
      }
    }
  }
}

inline many<a>(!p: zonk(a)) -> zonk(list(a)) {
  (k) =>> {
    let f =
      define self(acc: list(a)) ->> parsed(list(a)) {
        match p(k) {
        | Right(v) =>
          self(Cons(v, acc))
        | Left(_) =>
          Right(reverse(acc))
        }
      };
    f(Nil)
  }
}

inline some<a>(!p: zonk(a)) ->> zonk(list(a)) {
  (k) =>> {
    try v = p(k);
    try vs = many(p)(k);
    Right(Cons(v, vs))
  }
}

inline scan-while<a>(
  label: text,
  !p: (rune) -> bool,
  interpret: (&string) -> +?a,
) ->> zonk(a) {
  (k) =>> {
    let orig-cursor = _get-cursor(k);
    let end-cursor = _scan-while(k, p);
    _set-cursor(k, end-cursor);
    let size = sub-int(end-cursor, orig-cursor);
    let result =
      with-byte-slice(_get-input-stream(k), orig-cursor, size, (slice) => {
        interpret(slice)
      });
    match result {
    | Right(v) =>
      Right(v)
    | Left(_) =>
      _set-gap-size(k, max-int(size, 1));
      _set-expected-label(k, label);
      Left(Unit)
    }
  }
}
