data regex {
| Any(label: &string, runes: list(rune))
| Chunk(chunk-string: &string)
| Choose(label: &string, candidates: list(regex), fallback: regex)
| Join(components: list(regex))
| Repeat(regex)
| End-Of-Input
}
