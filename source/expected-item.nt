import {
  core.string.show {show-string-N},
}

data expected-item {
| _Raw-Text(&string)
| _Label(&string)
}

define _show-expected-item(k: expected-item) -> string {
  match k {
  | _Raw-Text(t) =>
    show-string-N(t)
  | _Label(t) =>
    *t
  }
}
