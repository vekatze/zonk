import {
  core.text.show {show-text-N},
}

data expected-item {
| Raw-Text(&text)
| Label(&text)
}

define show-expected-item(k: expected-item): text {
  match k {
  | Raw-Text(t) =>
    show-text-N(t)
  | Label(t) =>
    *t
  }
}
