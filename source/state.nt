import {
  this.pos {Pos, pos},
}

data state {
| State(
    stream: text,
    lastpos: pos,
    found: int,
  )
}

define new-state(input: text): cell(state) {
  new-cell(
    State of {
      stream = input,
      lastpos = Pos of {row = 0, column = 0},
      found = 0,
    },
  )
}
