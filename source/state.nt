import {
  this.point {Point, point},
}

data state {
| State(
    stream: text,
    cursor: point,
    found: int,
  )
}

define new-state(input: text): cell(state) {
  new-cell(
    State of {
      stream = input,
      cursor = Point of {row = 0, column = 0},
      found = 0,
    },
  )
}
