import {
  this.pos {Pos, pos},
}

data state {
| State(
    stream: text,
    cursor: pos,
    found: int,
  )
}

define new-state(input: text): cell(state) {
  new-cell(
    State of {
      stream = input,
      cursor = Pos of {row = 0, column = 0},
      found = 0,
    },
  )
}
