import {
  core::float.io {print-float},
  core::int.io {print-int, print-int-line},
  parse {
    end-of-input,
    make-parse-error,
    make-zonk-kit,
    report,
    zonk,
  },
  parse-preset {read-float-vector, read-int},
}

constant _sample-zonk: zonk(unit) {
  (k) =>> {
    try v1 = read-int(k);
    try v2 = read-int(k);
    try xs = read-float-vector(v1)(k);
    try _ = end-of-input(k);
    print-int(v1);
    print(", ");
    print-int-line(v2);
    core::vector.for(xs, (x, _) => {
      print-float(x);
      print(" ");
    });
    Right(Unit)
  }
}

define zen() -> unit {
  pin k = make-zonk-kit(*"6 42 \n   5.0 4.0 3.0 2.0 1.0 -1234.0");
  match _sample-zonk(k) {
  | Right(_) =>
    print("pass\n")
  | Left(_) =>
    let err = make-parse-error(k);
    pin message = report(err);
    print("fail: ");
    print-line(message);
  }
}
